[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "my-package"
version = "0.1.0"
description = "Programming portfolio for COMP5003"
requires-python = ">=3.8"
dependencies = ["requests>=2.28"]